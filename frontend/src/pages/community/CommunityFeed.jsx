import EmptyState from "../../components/EmptyState.jsx";

export default function CommunityFeed() {
  return <EmptyState title="This section is currently not active." message="Networkers is focused on connections, client leads, referrals, meetups, and opportunities." />;
}
