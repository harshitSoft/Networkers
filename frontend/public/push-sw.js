self.addEventListener("push",event=>{let data={};try{data=event.data.json()}catch{data={title:"Networkers",body:event.data?.text()||"You have a new notification"}}event.waitUntil(self.registration.showNotification(data.title||"Networkers",{body:data.body,icon:"/brand/networkers-logo-dark.png",badge:"/brand/networkers-logo-dark.png",data:{url:data.url||"/notifications"},vibrate:[150,80,150]}))});self.addEventListener("notificationclick",event=>{event.notification.close();const url=event.notification.data?.url||"/notifications";event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(list=>{for(const client of list)if("focus"in client){client.navigate(url);return client.focus()}return clients.openWindow(url)}))});
